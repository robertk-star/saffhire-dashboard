import Link from "next/link";
import { notFound } from "next/navigation";
import { AiReviewPanel } from "@/components/AiReviewPanel";
import { AppHeader } from "@/components/AppHeader";
import { getCaseBundle, getReviewTypeLabel, getStatusBadgeClass, getStatusLabel } from "@/lib/cases";
import { requireUser } from "@/lib/session";

export default async function CaseDetailPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ ai_error?: string }> }) {
  const user = await requireUser();
  const { id } = await params;
  const query = await searchParams;
  const { item, latestReview, decisions } = await getCaseBundle(id);
  if (!item) notFound();
  const decisionRows = [["reportable", "Reportable", "closed"], ["not_reportable", "Not Reportable", "closed"], ["needs_county_verification", "Needs County Verification", "open"], ["needs_more_information", "Needs More Information", "open"], ["needs_supervisor_review", "Needs Supervisor Review", "needs_supervisor_review"], ["possible_non_match_do_not_use", "Possible Non-Match / Do Not Use", "closed"]];
  return <><AppHeader user={user} /><main className="container-shell"><Link href="/cases" style={{ color: "#0f3b5f", fontWeight: 700 }}>← Back to cases</Link>{query.ai_error ? <div className="card" style={{ background: "#fff4cc", borderColor: "#f3c846", marginTop: 16, padding: 14 }}><strong>AI review issue:</strong> AI review could not be completed. The case is still saved and can be reviewed manually.</div> : null}<section className="card" style={{ marginTop: 16, padding: 22 }}><div style={{ alignItems: "start", display: "flex", justifyContent: "space-between", gap: 16 }}><div><h1 style={{ margin: "0 0 8px" }}>{item.subject_name}</h1><p style={{ color: "#5d687b", margin: 0 }}>{getReviewTypeLabel(item.review_type)}</p></div><span className={getStatusBadgeClass(item.status)}>{getStatusLabel(item.status)}</span></div><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 14, marginTop: 20 }}><div><strong>DOB</strong><br />{item.dob || "Not entered"}</div><div><strong>Client</strong><br />{item.client_name || "Not entered"}</div><div><strong>Jurisdiction</strong><br />{item.jurisdiction || "Not entered"}</div><div><strong>County/State</strong><br />{[item.county, item.state].filter(Boolean).join(", ") || "Not entered"}</div><div><strong>Source</strong><br />{item.source || "Not entered"}</div><div><strong>Reference</strong><br />{item.external_reference_number || "Not entered"}</div></div><div style={{ display: "flex", gap: 12, marginTop: 18 }}><Link className="btn-secondary" href={`/cases/${item.id}/summary`}>Print Review Summary</Link></div></section><AiReviewPanel caseId={item.id} latestReview={latestReview} /><section className="card" style={{ marginTop: 18, padding: 22 }}><h2 style={{ marginTop: 0 }}>Pasted Source Text</h2><pre style={{ background: "#f8fafc", borderRadius: 12, overflowX: "auto", padding: 16, whiteSpace: "pre-wrap" }}>{item.raw_record_text}</pre></section><section className="card" style={{ marginTop: 18, padding: 22 }}><h2 style={{ marginTop: 0 }}>Human Review Decision</h2><form action={`/api/cases/${item.id}/decision`} method="post" style={{ display: "grid", gap: 14 }}><select className="field-input" name="decision" required>{decisionRows.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><select className="field-input" name="next_status" required>{decisionRows.map(([value, label, status]) => <option key={value} value={status}>{label} → {getStatusLabel(status)}</option>)}</select><textarea className="field-input" name="note" placeholder="Required decision note" required /><button className="btn-primary" type="submit">Save Decision</button></form>{decisions.length ? <div style={{ marginTop: 18 }}><strong>Decision History</strong>{decisions.map((decision: any) => <pre key={decision.id} style={{ background: "#f8fafc", borderRadius: 12, padding: 12, whiteSpace: "pre-wrap" }}>{new Date(decision.created_at).toLocaleString()} — {decision.decision}\n{decision.note}</pre>)}</div> : null}</section><section style={{ display: "flex", gap: 12, marginTop: 18 }}><form action={`/api/cases/${item.id}/archive`} method="post"><button className="btn-muted" type="submit">Archive</button></form>{user.role === "admin" ? <form action={`/api/cases/${item.id}/remove`} method="post"><button className="btn-danger" type="submit">Soft Remove</button></form> : null}</section></main></>;
}
