import Link from "next/link";
import { AppHeader } from "@/components/AppHeader";
import { listTazworksClients, normalizeTazworksClientList } from "@/lib/tazworks";
import { requireUser } from "@/lib/session";
import { listTazworksSavedClients } from "@/lib/tazworksSavedClients";

export default async function TazworksClientsPage({ searchParams }: { searchParams: Promise<{ error?: string; created?: string; updated?: string }> }) {
  const user = await requireUser(["admin", "supervisor"]);
  const params = await searchParams;
  let clients = [] as ReturnType<typeof normalizeTazworksClientList>;
  let savedClients: any[] = [];
  let error = params.error || "";
  try {
    const data = await listTazworksClients(0, 100);
    clients = normalizeTazworksClientList(data);
  } catch (err: any) {
    error = err?.message || "client_pull_failed";
  }
  try { savedClients = await listTazworksSavedClients(true); } catch {}
  return <><AppHeader user={user} /><main className="container-shell"><h1 style={{ marginTop: 0 }}>TazWorks Clients</h1><p style={{ color: "#5d687b" }}>Choose a saved client or a live TazWorks client before pulling orders.</p>{params.created ? <p style={{ color: "#167f49", fontWeight: 700 }}>Saved client added.</p> : null}{params.updated ? <p style={{ color: "#167f49", fontWeight: 700 }}>Saved client updated.</p> : null}{error ? <div className="card" style={{ borderColor: "#fda29b", background: "#fff5f5", color: "#b42318", fontWeight: 700, marginBottom: 18, padding: 16 }}>Could not pull live clients. You can still use saved clients or manually enter a client GUID on Pull Orders.</div> : null}<section className="card" style={{ padding: 18, marginBottom: 18 }}><h2 style={{ marginTop: 0 }}>Add Saved Client</h2><form action="/api/tazworks/saved-clients/create" method="post" style={{ display: "grid", gap: 12 }}><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 12 }}><label><span className="field-label">Client Name</span><input className="field-input" name="name" required /></label><label><span className="field-label">Client Code</span><input className="field-input" name="clientCode" /></label><label><span className="field-label">Client GUID</span><input className="field-input" name="clientGuid" required /></label></div><label><span className="field-label">Notes</span><textarea className="field-input" name="notes" rows={2} /></label><button className="btn-primary" type="submit">Save Client</button></form></section><section className="card table-wrap" style={{ marginBottom: 18 }}><div style={{ padding: 18 }}><h2 style={{ margin: 0 }}>Saved Clients</h2></div><table><thead><tr><th>Client</th><th>Code</th><th>Status</th><th>Action</th></tr></thead><tbody>{savedClients.length ? savedClients.map((client: any) => <tr key={client.id}><td>{client.name}</td><td>{client.client_code || ""}</td><td>{client.is_active ? "Active" : "Inactive"}</td><td><div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}><Link style={{ color: "#0f3b5f", fontWeight: 800 }} href={`/tazworks/orders?clientGuid=${encodeURIComponent(client.client_guid)}&clientName=${encodeURIComponent(client.name)}&clientCode=${encodeURIComponent(client.client_code || "")}`}>Select Client</Link><form action="/api/tazworks/saved-clients/status" method="post"><input type="hidden" name="id" value={client.id} /><input type="hidden" name="isActive" value={client.is_active ? "false" : "true"} /><button className="btn-secondary" type="submit">{client.is_active ? "Deactivate" : "Activate"}</button></form></div></td></tr>) : <tr><td colSpan={4}>No saved clients yet.</td></tr>}</tbody></table></section><section className="card table-wrap"><div style={{ padding: 18 }}><h2 style={{ margin: 0 }}>Live TazWorks Clients</h2><p style={{ color: "#5d687b" }}>{clients.length} client{clients.length === 1 ? "" : "s"} returned by the API.</p></div><table><thead><tr><th>Client</th><th>Code</th><th>GUID</th><th>Action</th></tr></thead><tbody>{clients.length ? clients.map((client) => <tr key={client.guid}><td>{client.name}</td><td>{client.code || ""}</td><td style={{ fontSize: 12 }}>{client.guid}</td><td><Link style={{ color: "#0f3b5f", fontWeight: 800 }} href={`/tazworks/orders?clientGuid=${encodeURIComponent(client.guid)}&clientName=${encodeURIComponent(client.name)}&clientCode=${encodeURIComponent(client.code)}`}>Select Client</Link></td></tr>) : <tr><td colSpan={4}>No live clients returned.</td></tr>}</tbody></table></section></main></>;
}
